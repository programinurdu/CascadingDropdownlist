@using CascadingDropdownlist.ViewModels.Students

@Html.DropDownList("", new SelectList(StudentViewModel.GetCitiesAccordingToStates(Convert.ToInt32(ViewData["StateId"])), "Value", "Text", ViewData["State"]), string.Empty,
    new
    {
        @class = "form-control",
        @disabled = "disabled",
        @onchange = "document.forms[0].submit();"
    })
